using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ViveLazor : MonoBehaviour {

    protected Vector3 offsetTranslation = new Vector3(0, 0, 0.05f);
    protected Quaternion offsetRotation = Quaternion.Euler(20, 0, 0);

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
